[pytest]
# app/ nao e um pacote; os modulos se importam pelo nome (midia, prompt, ...).
pythonpath = app
testpaths = app/tests
markers =
    slow: carrega o modelo Whisper (~30s e ocupa a GPU); rode com -m slow
addopts = -m "not slow"
